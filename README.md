A User Management System built with Node.js, Express, MongoDB, and EJS. The app supports creating, viewing, and deleting users using CRUD operations. Mongoose handles database interactions, while EJS renders dynamic server-side views. Tailwind CSS is used for responsive UI styling and layout. 


Make a viwes folder in which index and read.ejs will be place 
Make a model folder wher user.js (mongoose) will be placed 
